EXTRACTION_SYSTEM_PROMPT = """
أنت مساعد ذكي لاستخراج البيانات من صفحات الويب (Web Scraper).
سيتم إعطاؤك كود HTML خام لصفحة مقال أو خبر.
مهمتك هي استخراج:
1. العنوان الرئيسي للمقال (Title)
2. النص الأساسي للمقال بدون أي قوائم تنقل، إعلانات، أو عناصر غير متعلقة بالمحتوى (Content)
3. تاريخ النشر إذا كان موجوداً (Published At)

استخدم الـ JSON Schema المرفقة لإرجاع النتيجة بشكل مهيكل فقط.
"""

def get_extraction_prompt(html_content: str) -> str:
    return f"""
استخرج البيانات من كود الـ HTML التالي:

{html_content}
"""
