[build]
buildCommand = "pip install -r requirements.txt && export PLAYWRIGHT_BROWSERS_PATH=0 && playwright install chromium"

[deploy]
startCommand = "cd backend && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
