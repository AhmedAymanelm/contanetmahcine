[build]
builder = "docker"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "bash -c 'cd backend && alembic upgrade head && python seed_admin.py && uvicorn app.main:app --host 0.0.0.0 --port $PORT'"
